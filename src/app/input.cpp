#include "../../include/app/input.h"

// This compilation unit is intentionally empty as InputState is a header-only POD struct.
// Logic for mouse bounds checking is currently handled locally in Renderers or App.